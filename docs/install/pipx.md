# 使用 pipx 安装

[English backup](pipx_en.md)

uv 是默认方案。组织已标准化 pipx 时，可以安装当前六技能版本：

```bash
pipx install --force \
  git+https://github.com/EuphoriaYan/spec-kit.git@v0.12.5+teamwork.3
specify --version
```

然后按[安装指南](../installation.md)初始化真实代码仓。团队应统一使用该固定 tag。
