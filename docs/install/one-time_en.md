# One-Time Trial

[中文主文档](one-time.md)

Run without changing the persistent `specify` on `PATH`:

```bash
uvx --from git+https://github.com/EuphoriaYan/spec-kit.git@v0.12.5+teamwork.2 \
  specify init . --integration codex
```

Use this for temporary samples only. Teams should standardize one commit, tag,
or internally published wheel.
